import { ComunidadIntegrantes } from './comunidad-integrantes';

describe('ComunidadIntegrantes', () => {
  it('should create an instance', () => {
    expect(new ComunidadIntegrantes()).toBeTruthy();
  });
});
